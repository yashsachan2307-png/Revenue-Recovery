{
  "name": "revenue-recovery-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "start": "cd backend && npm start"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ]
}
